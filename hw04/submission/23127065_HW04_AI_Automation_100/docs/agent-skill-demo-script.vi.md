# Kịch bản video Agent Skill

1. Chạy `whoami` và `hostname`.
2. Mở `.agents/skills/automation-test/SKILL.md`, giải thích workflow data-driven, 3 browser, report và defect triage.
3. Chọn feature FR-09 làm demo hoàn chỉnh: mở 12 case, `coupon.json`, và `coupon.spec.ts`.
4. Chạy lần lượt FR-09 trên Chromium, Firefox và WebKit hoặc chạy matrix đầy đủ.
5. Mở ba report `reports/html/coupon/*/index.html` và kiểm tra student ID/timestamp.
6. Chạy script `audit-assignment.mjs` từ README và chỉ ra `errors: []`.
7. Mở `evidence/bugs/index.json`, screenshot percentage calculation, và GitHub issue #27.
8. Giải thích skill đã được cập nhật trong thư mục có sẵn, không tạo skill trùng lặp.
