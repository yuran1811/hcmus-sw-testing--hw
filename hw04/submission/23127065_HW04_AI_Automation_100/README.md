# 23127065 — HW04 AI Automation

Task 1 automation for FR-06, FR-09, and FR-17. Install dependencies, start EShop's backend (`:3000`), web app (`:5173`), and admin app (`:5174`), then run:

```sh
npm install
npx playwright install
npm run list
npm run run:matrix
```

Each report carries `Run by: 23127065`; matrix status is saved in `reports/run-manifest.json`.
