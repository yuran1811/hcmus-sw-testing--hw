# Self-drawn diagram requirement

The assignment explicitly forbids an AI-generated diagram. No diagram has been fabricated.

`TODO(HUMAN)`: draw the architecture yourself from `pseudocode.md`, export it as `23127065-api-test-generator.png`, and place it in this directory. Show at least these decisions in your own visual layout: specification reader, real-route verifier, partition/security/state generators, human audit gate, student extension, exporters, Newman executor, and defect grouper.

Record the drawing tool and date in `Main_Report.md`, then add the image there.
